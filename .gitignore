.log/
.bin/
bin/
!npm/bin/
.DS_Store
/niq
# goreleaser output at the repo root. NOT the embedded webui dist —
# app/webui/assets/dist is go:embed'd and must be committed, so it
# deliberately stays unignored (a rebuild only changes it when the UI
# actually changed: vite bundles are content-hashed).
/dist/

# frontend dependencies / build caches
app/webui/assets/node_modules/

# local ad-hoc probe (a throwaway main package for testing a two-project niw
# link by hand); kept out of the repo.
scratch-probe/
